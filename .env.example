# Estas dos SÍ son públicas por diseño: la anon key de Supabase solo permite
# leer la vista public_profiles e insertar un prode nuevo (ver schema.sql RLS).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# La contraseña de admin NUNCA va acá ni con prefijo VITE_.
# Vive solo como variable de entorno de la Edge Function (server-side).
